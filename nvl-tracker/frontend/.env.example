# URL backend API (đặt trong Vercel > Project Settings > Environment Variables)
NEXT_PUBLIC_API_URL=http://localhost:8000
# Dùng cho route /api/telegram gọi backend (server-side); mặc định = NEXT_PUBLIC_API_URL
BACKEND_API_URL=http://localhost:8000
# URL public của dashboard (hiện trong lệnh /baocao)
NEXT_PUBLIC_SITE_URL=https://nvl.vercel.app

# --- Telegram webhook 2 chiều ---
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_WEBHOOK_SECRET=chuoi-bi-mat-ngau-nhien

# --- Kích hoạt GitHub Actions cho lệnh nặng /capnhat ---
GH_DISPATCH_PAT=ghp_xxx          # PAT scope 'repo' (hoặc fine-grained: Contents+Actions)
GH_OWNER=mrchinh189
GH_REPO=Scrapling
